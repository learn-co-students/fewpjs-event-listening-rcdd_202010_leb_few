const input = document.getElementById('input');

function addingEventListener(event) {
  const input = document.getElementById('input');
input.addEventListener('click', function(event) {
  alert('I was clicked!');
});
  
}

//input.addEventListener('click',addingEventListener(event));